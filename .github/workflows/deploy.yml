# 部署由 Railway 自动处理（push to main 触发）
# 详见 backend/railway.toml 和 frontend/railway.toml
name: Deploy (disabled - using Railway)

on:
  workflow_dispatch:  # 仅手动触发，禁止自动运行

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deployment handled by Railway Git integration"
