[build]
  builder = "dockerfile"
  dockerfilePath = "Dockerfile"

[deploy]
  startCommand = "sh -c 'npx vite preview --port ${PORT} --host'"
  restartPolicyType = "ON_FAILURE"
  restartPolicyMaxRetries = 3
